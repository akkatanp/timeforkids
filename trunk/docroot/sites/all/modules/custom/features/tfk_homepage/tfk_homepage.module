<?php
/**
 * @file
 * Code for the tfk_homepage feature.
 */

include_once('tfk_homepage.features.inc');

/**
 * Implements hook_ctools_plugin_directory().
 */
function tfk_homepage_ctools_plugin_directory($module, $plugin) {
	if (($module == 'draggableviews') && ($plugin == 'handler')) {
		return 'handlers';
	}
}
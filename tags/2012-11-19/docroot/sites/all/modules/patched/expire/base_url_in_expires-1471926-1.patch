From 392093e8827eada86d1b443ed54a8c8439a82d32 Mon Sep 17 00:00:00 2001
From: Pierre Buyle <buyle@pheromone.ca>
Date: Wed, 7 Mar 2012 11:47:24 -0500
Subject: [PATCH] Issue #1471926: Invalid expire URLs when "Include base URL
 in expires" is enabled.

---
 expire.module |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/expire.module b/expire.module
index 3026c3b..197422a 100644
--- a/expire.module
+++ b/expire.module
@@ -435,7 +435,7 @@ function expire_cache_derivative($paths, &$node = NULL) {
   $urls = array();
   global $base_url;
   if (variable_get('expire_include_base_url', EXPIRE_INCLUDE_BASE_URL)) {
-    $base_urls[] = $base_url;
+    $base_urls[] = $base_url . '/';
     if (module_exists('domain')
      && module_load_include('inc', 'expire', 'expire.domain') != FALSE) {
       $base_urls = array_merge($base_urls, expire_get_base_urls($node));
-- 
1.7.8.3

